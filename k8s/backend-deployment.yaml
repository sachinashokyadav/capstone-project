apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: capstone
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      nodeSelector:
        role: backend
      imagePullSecrets:
      - name: ecr-secret
      containers:
      - name: backend
        image: 688372068524.dkr.ecr.ap-south-1.amazonaws.com/capstoneproject/docker-images:backend-41135eb1c2a074fcd0acd671d17257d0a703382f
        ports:
        - containerPort: 3500   # ✅ Corrected port
        env:
        - name: PORT
          value: "3500"
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: capstone
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000        # ✅ Frontend will still call this
      targetPort: 3500  # ✅ Match backend container port
  type: ClusterIP
